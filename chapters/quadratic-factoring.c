/***
quadratic-factoring.c
a simple program to factor a standard-form quadratic
***/

#include <stdio.h>

int main(void) {
    float a, b, c, d;

    printf("standard-form quadratic factoring calculator\n");
    printf("enter a, b and c: ");
    scanf("%f%f%f", &a, &b, &c);
    printf("ok, I got 'a = %f', 'b = %f', and 'c = %f'. continuing.\n", a, b, c);

    if (a == 1) {
        
    }
}